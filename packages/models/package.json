{
  "name": "@pnpm-nx-monorepo/models",
  "version": "1.0.0",
  "description": "A library for data models.",
  "main": "dist/index",
  "types": "src/index",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "eslint": "^9.14.0",
    "typescript": "^5.6.3"
  }
}
