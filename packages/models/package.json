{
  "name": "@pnpm-nx-monorepo/models",
  "version": "1.0.0",
  "description": "A library for data models.",
  "main": "dist/index",
  "types": "src/index",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "typescript": "^5.6.3"
  }
}
