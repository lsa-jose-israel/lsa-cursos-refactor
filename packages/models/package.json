{
  "name": "@pnpm-nx-monorepo/models",
  "version": "1.0.0",
  "description": "A library for data models.",
  "main": "dist/index",
  "types": "src/index",
  "scripts": {
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
